# Pipeline Archive

This folder keeps old command-path wrappers and superseded workflow scripts for traceability.

Archived files are not active command paths. Prefer the live packages under:

```text
pipelines/news/benzinga/
pipelines/sec/edgar/
pipelines/reference_data/
```

