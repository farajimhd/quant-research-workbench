"""Reusable SEC EDGAR pipeline components.

The package is the shared layer for SEC historical fills and the live SEC
gateway. Existing one-off scripts should gradually become thin CLIs over these
modules.
"""
