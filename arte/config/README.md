# Configuration

Future schemas cover strategy requirements, account mandates, data coverage,
retention, latency policy, service versions, and laptop/workstation resource profiles.

Do not add plausible-looking executable defaults before their gates pass. Profiles
must reference external secret and runtime locations. No secrets belong here.
Configuration is an engine input, not a parent-app registry dependency.
