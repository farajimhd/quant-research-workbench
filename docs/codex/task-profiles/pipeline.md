# Pipeline task profile

Trace source acquisition, raw artifacts, normalization, canonical writes, checkpoints, resume behavior, and downstream consumers. Validate a representative source-to-canonical path and explicit counts for completed, skipped, retried, and failed units.
