[project]
name = "news-hypothesis"
version = "0.1.0"
description = "Point-in-time contextual news hypothesis service."
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "pydantic",
  "uvicorn",
]
