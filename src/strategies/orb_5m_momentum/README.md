# ORB 5-Minute Momentum Strategy

This folder is versioned. Strategy behavior, default config, and README files live under version subfolders.

## Versions

- `v1`: Current ORB 5-minute momentum implementation. It uses prior daily context for average volume, ATR, and previous close.

The compatibility modules at this folder level currently re-export `v1`.
